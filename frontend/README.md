# Frontend

Angular 20 application. Not scaffolded yet - see `project.md` build order:
the Models tab starts once the schema has settled.

```bash
npm install && npm start   # once scaffolded
npx orval                  # regenerate the API client from the backend OpenAPI schema
```
